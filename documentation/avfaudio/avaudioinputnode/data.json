{
  "path": "/documentation/avfaudio/avaudioinputnode",
  "type": "Class",
  "name": "AVAudioInputNode",
  "desc": "An object that connects to the system’s audio input.",
  "items": [
    {
      "name": "func setManualRenderingInputPCMFormat(AVAudioFormat, inputBlock: AVAudioIONodeInputBlock) -> Bool",
      "desc": "Supplies the data through the input node to the engine while operating in the manual rendering mode."
    },
    {
      "name": "typealias AVAudioIONodeInputBlock",
      "desc": "The type that represents a block to render operation calls to get input data when in manual rendering mode."
    },
    {
      "name": "var isVoiceProcessingInputMuted: Bool",
      "desc": "A Boolean that indicates whether the input of the voice processing unit is in a muted state."
    },
    {
      "name": "var isVoiceProcessingBypassed: Bool",
      "desc": "A Boolean that indicates whether the node is bypassing all processing of the voice processing unit."
    },
    {
      "name": "var isVoiceProcessingAGCEnabled: Bool",
      "desc": "A Boolean that indicates whether automatic gain control on the processed microphone or uplink signal is active."
    }
  ],
  "declaration": "class AVAudioInputNode : AVAudioIONode",
  "inheritsFrom": [
    "AVAudioIONode"
  ],
  "conformsTo": [
    "AVAudioMixing"
  ]
}