{
  "path": "/documentation/foundation/urlsessionwebsocketdelegate",
  "type": "Protocol",
  "name": "URLSessionWebSocketDelegate",
  "desc": "A protocol that defines methods that URL session instances call on their delegates to handle task-level events specific to WebSocket tasks.",
  "items": [
    {
      "name": "func urlSession(URLSession, webSocketTask: URLSessionWebSocketTask, didOpenWithProtocol: String?)",
      "desc": "Tells the delegate that the WebSocket task successfully negotiated the handshake with the endpoint, indicating the negotiated protocol."
    },
    {
      "name": "func urlSession(URLSession, webSocketTask: URLSessionWebSocketTask, didCloseWith: URLSessionWebSocketTask.CloseCode, reason: Data?)",
      "desc": "Tells the delegate that the WebSocket task received a close frame from the server endpoint, optionally including a close code and reason from the server."
    }
  ],
  "declaration": "protocol URLSessionWebSocketDelegate",
  "inheritsFrom": [
    "URLSessionTaskDelegate"
  ]
}