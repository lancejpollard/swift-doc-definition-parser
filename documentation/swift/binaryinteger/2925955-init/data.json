{
  "path": "/documentation/swift/binaryinteger/2925955-init",
  "type": "Generic Initializer",
  "name": "init(exactly:)",
  "desc": "Creates an integer from the given floating-point value, if it can be represented exactly.Required. Default implementations provided.",
  "items": [
    {
      "name": "init?<T>(exactly: T)",
      "desc": "Creates an integer from the given floating-point value, if it can be represented exactly."
    },
    {
      "name": "init?<T>(exactly: T)",
      "desc": "Creates a new instance from the given integer, if it can be represented exactly."
    },
    {
      "name": "init?<T>(exactly: T)",
      "desc": "Creates a new instance from the given integer, if it can be represented exactly."
    }
  ],
  "declaration": "init?<T>(exactly source: T) where T : BinaryFloatingPoint"
}