{
  "path": "/documentation/apple_news/articlelink",
  "type": "Object",
  "name": "ArticleLink",
  "desc": "The container component for creating a link to an article.",
  "items": [
    {
      "name": "articleIdentifier",
      "desc": "The shareable URL or CloudKit ID of an article that is navigated to, and that is used as the default title and thumbnail. If articleIdentifier is not provided, the identifier of the article is used."
    },
    {
      "name": "role",
      "desc": "Always article_link for this component."
    },
    {
      "name": "additions",
      "desc": "An array of ComponentLink objects. additions is automatically added to the ArticleLink container. Any link additions defined here or in any of the child components (ArticleTitle and ArticleThumbnail) have no effect."
    },
    {
      "name": "allowAutoplacedAds",
      "desc": "A Boolean value that allows the placement of ad banners between components. Nested components inherit the value of the outermost container that explicitly sets allowAutoplacedAds. The default value is false."
    },
    {
      "name": "anchor",
      "desc": "An object that defines vertical alignment with another component."
    },
    {
      "name": "animation",
      "desc": "An object that defines an animation to be applied to the component.The none value is used for conditional design elements. Adding it here has no effect."
    },
    {
      "name": "behavior",
      "desc": "An object that defines behavior for a component, like Parallax or Springy.The none value is used for conditional design elements. Adding it here has no effect."
    },
    {
      "name": "components",
      "desc": "An array of components to display as child components. Child components are positioned and rendered relative to their parent component."
    },
    {
      "name": "conditional",
      "desc": "An instance or array of container properties that can be applied conditionally, and the conditions that cause them to be applied."
    },
    {
      "name": "contentDisplay",
      "desc": "An object that defines how to position child components within this articleLink component. A HorizontalStackDisplay, for example, allows for displaying child components side by side.In versions of News prior to iOS 11, child components are positioned as if contentDisplay were not defined.The none value is used for conditional design elements. Adding it here has no effect."
    },
    {
      "name": "hidden",
      "desc": "A Boolean value that determines whether the component is hidden."
    },
    {
      "name": "identifier",
      "desc": "An optional unique identifier for this component. If used, this identifier must be unique across the entire document. You will need an identifier for your component if you want to anchor other components to it. See Anchor."
    },
    {
      "name": "layout",
      "desc": "An inline ComponentLayout object that contains layout information, or a string reference to a ComponentLayout object that is defined at the top level of the document. If layout is not defined, size and position are based on various factors, such as the device type, the length of the content, and the role of this component."
    },
    {
      "name": "style",
      "desc": "An inline ComponentStyle object that defines the appearance of this component, or a string reference to a ComponentStyle object that is defined at the top level of the document.The none value is used for conditional design elements. Adding it here has no effect."
    }
  ],
  "inheritsFrom": [
    "Container"
  ]
}